services:
  nginx:
    image: nginxinc/nginx-unprivileged:${NGINX_VERSION:-alpine}
    restart: unless-stopped
    user: "101:101"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:8080"
      - "443:8443"
