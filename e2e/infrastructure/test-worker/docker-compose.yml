services:
  ci-test-worker:
    image: ghcr.io/proxvex/ci-test-worker:latest
    restart: unless-stopped
    ports:
      - "2222:22"
    environment:
      SSH_PUBLIC_KEY: "{{ CI_WORKER_SSH_PUBLIC_KEY }}"
      SSH_PRIVATE_KEY: "{{ CI_WORKER_SSH_PRIVATE_KEY }}"
