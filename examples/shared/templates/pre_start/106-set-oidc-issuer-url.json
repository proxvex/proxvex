{
  "name": "Set OIDC Issuer URL",
  "description": "Set the external OIDC issuer URL for production. Override the default URL to match your Zitadel instance.",
  "commands": [
    {
      "properties": {
        "id": "oidc_issuer_url",
        "default": "https://auth.ohnewarum.de"
      }
    }
  ]
}
