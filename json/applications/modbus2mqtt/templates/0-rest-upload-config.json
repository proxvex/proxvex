{
  "name": "Upload modbus2mqtt settings",
  "description": "POSTs a 'Download Local Settings' ZIP (busses, slaves, local specifications) to the application's import endpoint after start. Bootstrap-only: refused with 409 if the local config dir is already populated. Tries unauthenticated first; on 401 falls back to OIDC client_credentials.",
  "execute_on": "lxc",
  "skip_if_all_missing": [
    "upload_modbus2mqtt_config_content"
  ],
  "parameters": [
    "upload_modbus2mqtt_config_content",
    "rest_url",
    "rest_path",
    "rest_payload_b64",
    "rest_content_type",
    "rest_health_path",
    "https_port",
    "oidc_issuer_url",
    "oidc_client_id",
    "oidc_client_secret"
  ],
  "commands": [
    {
      "name": "REST upload",
      "script": "post-rest-upload.sh",
      "library": "rest-configure-common.sh",
      "outputs": [
        "rest_uploaded"
      ]
    }
  ]
}
