{
  "task": "reconfigure",
  "depends_on": ["node-red/default"],
  "selectedAddons": ["addon-ssl"],
  "stack_name": "default",
  "wait_seconds": 30,
  "description": "Reconfigure node-red/default in place with the same SSL addon — exercises the reconfigure pipeline against an SSL-enabled container. stack_name='default' keeps the reconfigure in the baseline's stack so secrets/passwords stay consistent (cf. zitadel/reconf-ssl). Closes oci-image/ssl/reconfigure gap."
}
