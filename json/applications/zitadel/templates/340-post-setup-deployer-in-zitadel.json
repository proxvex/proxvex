{
  "name": "Setup Deployer in Zitadel",
  "description": "Creates proxvex project, OIDC app, and roles in Zitadel using the ephemeral admin PAT. Stores credentials in /bootstrap/deployer-oidc.json for downstream OIDC addon activation without API access.",
  "execute_on": "lxc",
  "parameters": [
    "hostname",
    "domain_suffix",
    "compose_project",
    "ssl_mode",
    "ZITADEL_EXTERNALDOMAIN"
  ],
  "commands": [
    {
      "name": "Setup Deployer in Zitadel",
      "script": "post-setup-deployer-in-zitadel.sh",
      "library": "pkg-common.sh",
      "outputs": [
        "oidc_issuer_url",
        "zitadel_project_id",
        "oidc_client_id",
        "oidc_client_secret"
      ]
    }
  ]
}
