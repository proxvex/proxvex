{
  "default": {
    "cleanup": {
      "postgres": "SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname='zitadel' AND pid<>pg_backend_pid(); DROP DATABASE IF EXISTS zitadel; DROP USER IF EXISTS zitadel;"
    },
    "cli_timeout": 900,
    "wait_seconds": 90,
    "params": [
      { "name": "setup_test_project", "value": "true" }
    ]
  },
  "ssl": {
    "cleanup": {
      "postgres": "SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname='zitadel' AND pid<>pg_backend_pid(); DROP DATABASE IF EXISTS zitadel; DROP USER IF EXISTS zitadel;"
    },
    "cli_timeout": 900,
    "wait_seconds": 90
  },
  "production": {
    "cleanup": {
      "postgres": "SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname='zitadel' AND pid<>pg_backend_pid(); DROP DATABASE IF EXISTS zitadel; DROP USER IF EXISTS zitadel;"
    },
    "cli_timeout": 900,
    "wait_seconds": 90
  }
}
