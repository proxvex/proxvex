{
  "execute_on": "ve",
  "name": "Check HTTPS Cert",
  "description": "From the PVE host, openssl-connect to <hostname>:<https_port> and assert: TLS handshake succeeds, cert SAN contains the expected hostname, issuer is the proxvex CA, expiry is more than 7 days away. Works for LXC services (via container hostname/IP) and for the PVE host itself (proxmox app) where https_port=8006.",
  "skip_if_all_missing": ["has_addon_ssl"],
  "parameters": [
    "vm_id",
    "hostname",
    "domain_suffix",
    "https_port",
    "ssl_mode"
  ],
  "commands": [
    {
      "name": "Check HTTPS Cert",
      "script": "host-check-https-cert.sh",
      "outputs": ["check_https_cert"]
    }
  ]
}
