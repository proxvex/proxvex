{
  "execute_on": "ve",
  "name": "Stop LXC Container",
  "description": "Stops the LXC container and waits until it is stopped",
  "parameters": [
    "vm_id"
  ],
  "commands": [
    {
      "script": "lxc-stop.sh"
    }
  ]
}
