{
  "execute_on": "ve",
  "name": "Configure LXC for Docker",
  "description": "Configure LXC container to support Docker (cgroup2, AppArmor, capabilities). Container must be stopped for configuration changes.",
  "parameters": [
    "vm_id"
  ],
  "commands": [
    {
      "script": "conf-configure-lxc-for-docker.sh",
      "description": "Add required LXC configuration for Docker support"
    }
  ]
}