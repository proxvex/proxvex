{
  "execute_on": "ve",
  "name": "Update /etc/hosts entries",
  "description": "Optional template to update /etc/hosts with hostname and static IP addresses from previous steps.",
  "skip_if_all_missing": [
    "static_ip",
    "static_ip6"
  ],
  "parameters": [
    "hostname",
    "static_ip",
    "static_ip6"
  ],
  "commands": [
    {
      "name": "lxc-update-etc-hosts",
      "script": "conf-lxc-update-etc-hosts.sh"
    }
  ]
}