{
  "name": "Set OIDC Issuer URL",
  "description": "Set the external OIDC issuer URL. Override this template in your local directory to set a default for all OIDC applications.",
  "commands": [
    {
      "properties": {
        "id": "oidc_issuer_url",
        "default": ""
      }
    }
  ]
}
