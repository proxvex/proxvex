{
  "execute_on": "ve",
  "name": "Register PVE Hookscript",
  "description": "Registers the proxvex hookscript on the Proxmox host. The hookscript runs on_start_container inside the container after each restart to start services that have no init system support.",
  "parameters": [
    "vm_id"
  ],
  "commands": [
    {
      "script": "conf-register-hookscript.sh",
      "description": "Create/update hookscript and register with container"
    }
  ]
}
