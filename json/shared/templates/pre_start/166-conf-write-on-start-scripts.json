{
  "execute_on": "ve",
  "name": "Write On-Start Scripts",
  "description": "Writes the on_start_container dispatcher and drop-in scripts (acme-renew, ssl-proxy) to the host-side volume directory. Files appear inside the container at /etc/proxvex/ via bind mount.",
  "parameters": [
    "hostname",
    "vm_id",
    "ssl_mode",
    "http_port",
    "https_port",
    "CF_TOKEN",
    "acme_san",
    "acme_email",
    "acme.cert_dir",
    "acme.needs_ca_cert",
    "acme_staging",
    "alpine_mirror",
    "debian_mirror",
    "compose_project",
    "uid",
    "gid"
  ],
  "commands": [
    {
      "script": "conf-write-on-start-scripts.sh",
      "library": "pve-common.sh",
      "description": "Write dispatcher and on_start.d scripts to host-side volume"
    }
  ]
}
