{
  "execute_on": "ve",
  "name": "Verify Container",
  "description": "Verifies container is running, notes contain managed marker, and LXC log has no errors.",
  "commands": [
    {
      "name": "Verify Container Status",
      "script": "host-verify-container.sh",
      "outputs": ["verify_results"]
    }
  ]
}
