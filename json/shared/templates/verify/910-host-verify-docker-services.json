{
  "execute_on": "ve",
  "name": "Verify Docker Services",
  "description": "Verifies all Docker services are running inside the container. Skips gracefully if docker is not installed.",
  "commands": [
    {
      "name": "Verify Docker Services",
      "script": "host-verify-docker-services.sh",
      "outputs": ["verify_services"]
    }
  ]
}
