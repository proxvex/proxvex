{
  "name": "Grant Test Deployer Roles",
  "description": "Iterates over every Zitadel project and binds all of its defined roles to the test-deployer machine user as UserGrants. Required so an OIDC access token issued for test-deployer carries the project-role claims, otherwise apps that enforce OIDC_REQUIRED_ROLE reject the dev-session login. Idempotent; safe to re-run after each new application deployment.",
  "execute_on": "lxc",
  "parameters": [
    {
      "id": "hostname",
      "name": "Hostname",
      "type": "string",
      "required": true,
      "description": "Hostname of this Zitadel instance"
    },
    {
      "id": "project_domain_suffix",
      "name": "Domain Suffix",
      "type": "string",
      "required": false,
      "default": "",
      "advanced": true,
      "description": "Domain suffix for constructing external URLs"
    },
    {
      "id": "ssl_mode",
      "name": "SSL Mode",
      "type": "string",
      "required": false,
      "advanced": true,
      "description": "SSL mode for protocol detection"
    }
  ],
  "commands": [
    {
      "name": "Grant Test Deployer Roles",
      "script": "post-grant-test-deployer-all-roles.sh"
    }
  ]
}
