{
  "name": "Set Project Parameters",
  "description": "Livetest override: route apk/apt through fast EU mirrors so Alpine/Debian pkg installs (Trust OCI CA, npm/pip post-installs) don't stall on dl-cdn.alpinelinux.org from inside the nested VM.",
  "commands": [
    {
      "properties": [
        { "id": "vm_id_start", "default": "100" },
        { "id": "oidc_issuer_url", "default": "" },
        { "id": "acme_san", "default": "" },
        { "id": "alpine_mirror", "value": "https://mirror1.hs-esslingen.de/Mirrors/alpine" },
        { "id": "debian_mirror", "value": "http://mirror.23m.com/debian/" }
      ]
    }
  ]
}
